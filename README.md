# Color AI-Detected Video Controller

Trained AI-model to detect colors in webcam feed and control video playback accordingly.

# To test

1. Clone the repository
2. Install dependencies: `npm install`
3. Start the development server: `npm run dev`

